# Vertex-Database
A custom-built database engine written in Python, featuring persistence, indexing, query support, and basic transaction handling. Designed as a lightweight, extensible system for managing structured data.
